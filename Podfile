# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PrayersWidgetExtension' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Moya', '~> 14.0'

  # Pods for PrayersWidgetExtension

end

target 'PrayerTime' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PrayerTime
  pod 'Moya', '~> 14.0'

  target 'PrayerTimeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PrayerTimeUITests' do
    # Pods for testing
  end

end
